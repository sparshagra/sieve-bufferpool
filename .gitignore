build/
results/*.db
results/*.csv
__pycache__/

# results/*.png is deliberately NOT ignored: the README embeds the charts, and
# they only render on GitHub if they are committed.
*.exe
*.o

# CMake artifacts
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
Testing/

# Python venv (Phase 4 plotting)
venv/
.venv/

# abandoned pre-C++ prototype, kept locally only
_old_python_attempt/
